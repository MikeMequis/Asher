<UserControl x:Class="Asher.UserInterface.Views.InstallationProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" VerticalAlignment="Center">
            <!--  Header  -->
            <materialDesign:Card Margin="0,0,0,24" Padding="24">
                <StackPanel>
                    <TextBlock Margin="0,0,0,8"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               Text="Instalando" />
                    <TextBlock Opacity="0.7"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               Text="Preparando o jogo para suporte a mods" />
                </StackPanel>
            </materialDesign:Card>

            <!--  Progress Card  -->
            <materialDesign:Card Padding="32">
                <StackPanel>
                    <!--  Progress Indicator  -->
                    <StackPanel Margin="0,0,0,24" HorizontalAlignment="Center">
                        <ProgressBar Width="200"
                                     Height="200"
                                     IsIndeterminate="{Binding IsIndeterminate}"
                                     Maximum="100"
                                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     Value="{Binding ProgressPercentage}" />
                        <TextBlock Margin="0,16,0,0"
                                   HorizontalAlignment="Center"
                                   FontSize="24"
                                   FontWeight="Medium"
                                   Text="{Binding ProgressPercentage, StringFormat={}{0:0}%}"
                                   Visibility="{Binding IsIndeterminate, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <!--  Status Text  -->
                    <TextBlock Margin="0,0,0,16"
                               HorizontalAlignment="Center"
                               FontSize="16"
                               Text="{Binding StatusMessage}"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />

                    <!--  Progress Bar (Linear)  -->
                    <ProgressBar Height="4"
                                 IsIndeterminate="{Binding IsIndeterminate}"
                                 Maximum="100"
                                 Value="{Binding ProgressPercentage}" />

                    <!--  Step Details  -->
                    <StackPanel Margin="0,24,0,0" Visibility="{Binding ShowStepDetails, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Margin="0,0,0,8"
                                   Opacity="0.7"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   Text="{Binding CurrentStepDetails}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!--  Info Card  -->
            <materialDesign:Card Margin="0,16,0,0" Padding="16">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Width="20"
                                              Height="20"
                                              Margin="0,0,8,0"
                                              VerticalAlignment="Center"
                                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                                              Kind="Information" />
                    <TextBlock VerticalAlignment="Center"
                               Opacity="0.6"
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Text="Por favor, não feche o aplicativo durante a instalação"
                               TextWrapping="Wrap" />
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
