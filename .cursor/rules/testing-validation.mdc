---
description: Testing and validation commands for Asher — canonical in the asher-testing skill
globs: "{Asher.Electron/scripts/**,Asher.Electron/package.json,Asher.Host.TestClient/**,Asher.Host/SmokeRunner.cs}"
alwaysApply: false
---

# Testing and Validation

Full command set and strategy: `asher-testing` skill.

Cursor reminder: smallest scope first (build the changed C# project, run the relevant smoke test);
real-game validation is required for install, uninstall, launch, or runtime-patching changes. Do not
claim success without running the required validation.
