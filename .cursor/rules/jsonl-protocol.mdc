---
description: JSONL protocol conventions between Electron and Asher.Host
globs: "{Asher.Host/Jsonl/**,Asher.Electron/src/main/jsonl-client.js,Asher.Electron/src/main/host-manager.js,Asher.Electron/src/renderer/application-client.js,Asher.Electron/src/preload/preload.cjs}"
alwaysApply: false
---

# JSONL Protocol

Electron spawns `Asher.Host --jsonl` as a child process. Communication is line-delimited JSON on stdin (requests) and stdout (responses/events).

## Request shape

```json
{ "requestId": "<uuid>", "method": "<method>", "params": { ... } }
```

## Response shape

```json
{ "requestId": "<id>", "success": true, "result": { ... } }
{ "requestId": "<id>", "success": false, "error": { "code": "<code>", "message": "<msg>" } }
```

## Events (no requestId)

```json
{ "event": "ready" }
{ "event": "progress", "requestId": "<id>", "progress": { ... } }
```

## Available methods

`getSettings`, `saveSettings`, `getApplicationMode`, `detectGameFolder`, `getGameFolderInfo`, `resolveGameFolderPath`, `isGameInstalled`, `hasRestorableBackup`, `getMods`, `setModEnabled`, `install`, `uninstall`, `launchGame`, `markInstalled`, `markUninstalled`, `cancel`, `shutdown`

## Error codes

`invalid_request`, `unknown_method`, `application_error`, `cancelled`, `not_found`, `internal_error`

## Rules

- Host stdout is exclusively for JSONL protocol traffic. Never write diagnostics to stdout.
- Every request must include `requestId` and `method`.
- Add new backend capabilities as new JSONL methods mapped through `IAsherApplication`, not ad-hoc channels.
- Preserve existing message shapes; do not silently change field names or semantics.
- Progress events reference the originating `requestId`.
- Cancellation uses the `cancel` method with `{ targetRequestId }`.
